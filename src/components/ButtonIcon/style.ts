import styled from "styled-components";

export const Button = styled.button`
  background-color: transparent;
  border: 0;
  border-radius: 5px;
  width: 29px;
  transition: 0.5s;
  margin-left: auto;
  cursor: pointer;
  svg {
    transition: 0.5s;
    margin-top: 4px;
    color: var(--grey-50)
  }
`